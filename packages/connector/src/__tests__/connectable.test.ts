import { Connectable } from "../connectable";

describe("Connectable", () => {
  const connectable = new Connectable("c");
  const data = "hello";

  beforeEach(() => {
    // connectable.clear();
  });
});
